{
  "name": "dynamic-form-system",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "turbo": "^1.10.0"
  }
}
